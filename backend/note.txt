🧠 BACKEND ARCHITECTURE (PROPER IMPLEMENTATION)

Use:

Node.js + Express + MongoDB (Mongoose)

Core Models (Mongoose Schemas)
User Model
name
email
password (hashed)
role (SuperAdmin / Admin / Employee)
companyId
department
riskScore
createdAt
Company Model
name
industry
adminId
employeeCount
Campaign Model
name
type (phishing / smishing / vishing)
companyId
targetEmployees
status
createdAt
Performance Model
userId
campaignId
clicked
reported
timeTaken
score
Backend APIs (FULL CRUD)
Auth Routes
POST /auth/register
POST /auth/login
POST /auth/logout
GET /auth/refresh
Employee Routes
GET /employees
POST /employees
PUT /employees/:id
DELETE /employees/:id
Campaign Routes
POST /campaigns
GET /campaigns
DELETE /campaigns/:id
PUT /campaigns/:id
Analytics Routes
GET /analytics/overview
GET /analytics/leaderboard
GET /analytics/department
🔄 DATABASE INTEGRATION (NO HARDCODE)

You MUST:

Remove all static/mock data
Fetch all data via API calls
Use real MongoDB collections
Seed Data System

Create a seeder script:

generate fake companies
generate employees
assign departments
assign random scores

Use faker.js

🎯 FRONTEND (PROPER INTEGRATION)

Use:

React + Tailwind + Framer Motion

State Management

Use:

Redux Toolkit OR Context API

Store:

user session
theme
sidebar state
API Layer

Create centralized API service:

/services/api.js

Use Axios instance:

base URL
interceptors
auth headers
📊 DASHBOARD FIX (IMPORTANT)

Redesign dashboard to match landing page theme.

Dashboard UI Must Include:
Top Cards
Total Employees
Active Campaigns
Risk Score
Phishing Click Rate

Each card:

animated numbers
gradient glow
hover animation
Charts Section
Line chart (click rate over time)
Bar chart (department risk)
Pie chart (attack types)

Use Recharts or Chart.js

🏆 LEADERBOARD PAGE

Fully dynamic from backend.

Display:

rank
name
department
score

Top 3:

gold / silver / bronze styling
crown icon
👥 EMPLOYEE MANAGEMENT PAGE

Must be fully functional:

Add Employee (modal form)
Edit Employee
Delete Employee
Search + Filter
🎯 CAMPAIGN PAGE

Allow admin to:

create phishing campaign
assign employees
simulate status

Buttons must work:

Create
Edit
Delete

🎨 UI CONSISTENCY (VERY IMPORTANT)

Apply:

glassmorphism cards
gradient borders
neon hover glow
smooth animations
🔥 ANIMATIONS

Use Framer Motion:

page transitions
sidebar slide
card hover
modal open/close
🌙 DARK / LIGHT MODE

Implement:

theme toggle
persistent preference
🚀 PERFORMANCE & UX

Add:

loading skeletons
error handling UI
toast notifications
🧩 FINAL GOAL

The system must behave like:

real SaaS product
not a static demo
not UI-only project